# Privacy

This software can process LINE messages and AI-extracted names, organizations, locations, dates, and activity descriptions. A deployment operator must define a lawful purpose, minimize collected fields, restrict dashboard access, establish retention and deletion rules, and disclose AI processing to affected users where required.

The repository must contain synthetic fixtures only. It must not contain production messages, user identifiers, access tokens, database exports, or evidence files.
